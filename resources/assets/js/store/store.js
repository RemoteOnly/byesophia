import Vue from 'vue'
import Vuex from 'vuex'
import {
    SET_IMAGE_DETAIL_LOADING_STATUS,
    SET_CURRENT_IMAGE,
    RESET_CURRENT_IMAGE,
    SET_CURRENT_IMAGE_COMMENTS,
    RESET_CURRENT_IMAGE_COMMENTS,
    SET_CURRENT_RELATED_IMAGES,
    PUSH_IMAGE_ID,
    SET_SHOULD_TO_LOAD_IMAGES
} from "./mutation-types";

Vue.use(Vuex);

export default new Vuex.Store({
    state: {

    },
    mutations: {

    }
});
